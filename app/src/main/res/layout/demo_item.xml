<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="presenter"
            type="com.danhtran.androidbaseproject.ui.activity.demo_database.items.DemoItemPresenter">

        </variable>
    </data>

    <LinearLayout
        android:id="@+id/lnParent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="10.6dp"
        android:layout_marginTop="8.2dp"
        android:layout_marginEnd="13.9dp"
        android:layout_marginBottom="8.2dp"
        android:orientation="vertical">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{String.valueOf(presenter.demoItemData.user.id)}" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{presenter.demoItemData.user.username}" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{presenter.demoItemData.user.phone}" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{presenter.address}" />

    </LinearLayout>
</layout>